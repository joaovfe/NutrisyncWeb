
  # Build the app

  This is a code bundle for Build the app. The original project is available at https://www.figma.com/design/GhHwHQA77mkX7B6Oirxb1E/Build-the-app.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  